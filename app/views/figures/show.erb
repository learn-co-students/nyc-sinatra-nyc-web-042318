<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%=@figure.name%></h1>

    <h3>Landmarks</h3>
    <%@figure.landmarks.each do |landmark|%>
      <p><%=landmark.name%></p>
    <%end %>

    <h3>Titles</h3>
    <%@figure.titles.each do |title| %>
      <p><%=title.name%></p>
    <%end %>
  </body>
</html>
